import styled from "styled-components";

const StyledMain = styled.main`
  display: flex;
  flex-direction: row;
  background-color: var(--White);
  width: 728px;
  height: 510px;
  box-shadow: 4px 4px 12px var(--Pale-blue);
  border-radius: 48px;
`;

export default StyledMain;
